# paged.data feature registry — roadmap namespaces (spec §12.3): automation,
# governed. All M1+ — `planned` rows (no coverage-gate obligation; the
# BREAKAGE_LOG carries the SDK dependencies). The data.provider.* role has
# its own gated file (registry/features/provider.yaml) now that its engine
# side is implemented.

- id: data.automation.batch
  title: Batch/headless generation (per-record / per-group / one catalog, §10)
  spec: plugin-data data-automation (base-idea §10 — RESERVED, M2)
  status: planned

# data.governed.catalog (the metadata-enrichment half) is implemented in its own
# gated file (registry/features/governed.yaml); this row is the broader
# end-to-end path (the bundle reading the table + sidecar from a file/URL/DB).
- id: data.governed.extract
  title: Governed-extract path (read governed tables + sidecar from a file/URL/DB location, §7)
  spec: plugin-data data-sources (base-idea §7, D-1/D-5 — M2)
  status: planned
